export interface AddressModel {
  id: number;
  period_start: string;
  period_end: string;
  address_id: string;
  formatted_address: string;
  apt_suite: string;
  comment: string;
  formattedAddress?: string;
}
