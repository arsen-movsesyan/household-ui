export interface CreateAddressModel {
  period_start: string;
  period_end: string;
  address_line1: string;
  address_line2: string;
  apt_suite: string;
  city: string;
  state: string;
  zip_code: string;
  comment: string;
}
